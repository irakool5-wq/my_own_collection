---
# Default values for my_own_module parameters
# These can be overridden when including the role

my_own_module_path: "/tmp/default_ansible_file.txt"
my_own_module_content: "Default content from my_own_role"
my_own_module_force: true
